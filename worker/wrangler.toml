name = "academic-homepage-visits"
main = "index.js"
compatibility_date = "2026-05-12"

# Create a KV namespace and replace the placeholder id:
# wrangler kv namespace create VISITS
kv_namespaces = [
  { binding = "VISITS", id = "replace-with-kv-namespace-id" }
]

# Set this secret before deployment:
# wrangler secret put DEDUPE_SALT

